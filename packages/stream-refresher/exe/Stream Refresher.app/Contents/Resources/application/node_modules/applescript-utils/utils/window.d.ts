declare type WaitForWindowProps = {
    windowName: string;
    processName: string;
    numAttempts?: number;
    /**
     * Interval to wait for before trying again in seconds (default 0.1)
     */
    interval?: number;
};
export declare function waitForWindow({ windowName, processName, numAttempts, interval, }: WaitForWindowProps): Promise<void>;
export {};
